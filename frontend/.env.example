# ── API Backend ───────────────────────────────────────────────────────────────
# URL backend Railway. WAJIB di-set di Vercel dashboard → Environment Variables.
# Tanpa ini, semua API call akan gagal di production (fallback ke localhost:8000).
NEXT_PUBLIC_API_URL=https://your-backend.up.railway.app
